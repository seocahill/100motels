<div class="row">
  <div class="span12">
    <div class="page-header">
      <h3><%= @event.artist %> in <%= @event.venue %> on <%= time_tag @event.date %></h3>
    </div>
  </div>
</div>
<div class="row">
  <div class="span12">
    <ul class="breadcrumb">
      <li><i class="icon-home"></i> <%= link_to "Organizer Home", organizer_root_path %><span class="divider">/</span></li>
      <li><%= link_to "All Events", organizer_events_path %><span class="divider">/</span></li>
      <li><a href="" class="active">Event Orders</a></li>
      <li></li>
    </ul>
  </div>
</div>
<div class="row">
  <div class="span12">
    <%= will_paginate @orders %>

    <%= form_tag  charge_multiple_orders_path, method: :post  do %>
      <div class="option-buttons">
        <% if current_user.profile && current_user.profile.api_key %>
          <%= link_to "Charge Selected", "#modal-charge", class: "btn btn-success", data: {toggle: "modal"}, id: "charge" %>
          <%= submit_tag "Refund Selected", name: 'refund', class: "btn btn-danger", id: "refund" %>
        <% elsif current_user.profile %>
          <%= link_to image_tag('blue.png'), user_omniauth_authorize_path(:stripe_connect), class: "" %>
        <% end %>
        <%= link_to "Generate pdf", organizer_event_path(@event, format: "pdf"), class: "btn" %>
      </div>
      <%= hidden_field_tag "organizer", current_user.profile.id %>
      <table class="table table-striped table-bordered" id="organizer-customers">
        <thead>
          <tr>
            <th>
              <button type="button" id="check_all", class="btn btn-mini">
                <i class="icon-ok"></i>
              </button>
            </th>
            <th>Customer Name</th>
            <th>Email</th>
            <th>Admits:</th>
            <th>Order Total</th>
            <th colspan="1" >Status</th>
          </tr>
        </thead>
        <tbody>
          <% @orders.each do |order| %>
          <tr>
            <td><%= check_box_tag "order_ids[]", order.id %> </td>
            <td><%= order.name %> </td>
            <td><%= order.email %> </td>
            <td> <%= order.quantity %></td>
            <td> <%= number_to_currency(order.total(@event)) %></td>
            <td> <span class="label"><%= order.stripe_event  %></span> </td>
            <% end %>
          </tr>
        </tbody>
      </table>
      <div class="modal hide fade" id="modal-charge" tabindex="-1" role="dialog" aria-hidden="true">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">x</button>
        <h3>Are you sure?</h3>
      </div>
      <div class="modal-body">
        <p>If you continue you will charge all your customers, the proceeds will be deposited into your bank account in seven days minus 100 Motel's and Stripe's fees which are <strong>non-refundable.</strong></p>
        <p>If this is your first event please make sure you have read the <a href="">Terms and Conditions</a> and the <a href="">Getting started guide</a></p>
        <p>We also strongly advise you to <a href="">sign a contract</a> with your Artists, a contract template is provided in the Getting started section.</p>
      </div>
      <div class="modal-footer">
        <a href="#" class="btn" data-dismiss="modal">Cancel</a>
        <%= submit_tag "Charge Selected", class: "btn btn-success", id: "charge" %>
      </div>
    </div>
    <% end %>
  </div>
</div>
