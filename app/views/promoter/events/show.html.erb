<div class="page-header"><h3><%= @event.artist %> in <%= @event.venue %> on <%= @event.date %></h3></div>

<h4>Customers</h4>


<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>Customer Name</th>
      <th>Email</th>
      <th>Ticket Admits:</th>
      <th>Total Price</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <% @orders.each do |order| %>
    <tr>
      <td><%= order.name %> </td>
      <td><%= order.email %> </td>
      <td>TODO </td>
      <td><%= order.total %> </td>
      <td> pending </td>
    </tr>
    <% end %>
  </tbody>
</table>