<!DOCTYPE html>
<html>
  <head>
    <title>
    	<% if content_for?(:title) %>
    		<%= yield(:title) %>
    	<% else %>
    		100 Motels
    	<% end %>
    </title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= filepicker_js_include_tag %>
    <%= csrf_meta_tags %>
    <!--if [lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body data-spy="scroll" data-target=".sidebar">
    <div class="navbar">
      <div class="navbar-inner">
        <div class="container" style="width: auto;">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to image_tag('ic_launcher.png') + "100 Motels", root_path, class: "brand brand-center" %>
          <%#{}= image_tag "ic_launcher.png", class: "brand" %>
          <div class="nav-collapse">
            <ul class="nav topbar" id="">
              <li class=""><%= link_to "Home", root_path %></li>
              <li><%= link_to "Events", events_path %></li>
              <li><%= link_to "Organizers", profiles_path %></li>
              <li><%= link_to "Help &Info", info_path %></li>
              <li><%= render 'events/search' %></li>
            </ul>
            <ul class="nav pull-right topbar">
              <% unless user_signed_in? %>
                <li><%= link_to "Sign up", new_user_registration_path %></li>
                <li><%= link_to "Sign in", new_user_session_path %></li>
              <% end %>
              <li class="divider-vertical"></li>
              <li class="dropdown">
                <% if user_signed_in? %>
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Organizer menu<b class="caret"></b></a>
                <% end %>
                <ul class="dropdown-menu">
                  <% if user_signed_in? %>
                    <% if current_user.has_role? :promoter %>
                    <li> <%= link_to "Admin", promoter_root_path %>  </li>
                    <li> <%= link_to "Public Profile", profile_path(current_user.profile) %>  </li>
                    <li> <%= link_to "Events", promoter_events_path %>  </li>
                    <% end %>
                  <% end %>
                </ul>
              </li>
              <li class="dropdown">
                <% if user_signed_in? %>
                <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.email %><b class="caret"></b></a>
                <% end %>
                <ul class="dropdown-menu">
                  <li><%= link_to "User Account", current_user %></li>
                  <li><%= link_to "Account Settings", edit_registration_path(current_user) %></li>
                    <li class="divider"></li>
                  <li><%= link_to "Sign out", destroy_user_session_path, :method => 'delete' %></li>
                </ul>
              </li>
            </ul>
          </div><!-- /.nav-collapse -->
        </div> <!-- container -->
      </div><!-- /navbar-inner -->
    </div>  <!-- /navbar -->
    <div class="container content">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key == :notice ? "success" : "error" %>">
          <a class="close" data-dismiss="alert">&#215;</a>
          <%= content_tag :div, value, :id => "flash_#{key}" if value.is_a?(String) %>
        </div> <!-- /flash -->
      <% end %>
      <%= yield %>
    </div> <!-- /content container -->
  </body>
  <footer>
    <div class="container">
      <div class="row footer-top">
        <div class="span3 border-divider">
          <dl>
            <dt>Help Pages</dt>
            <dd>ToS</dd>
            <dd>Legal</dd>
            <dd>Privacy</dd>
            <dd>Tech Support</dd>
          </dl>
        </div>
        <div class="span3 border-divider">
          <dl>
            <dt>How it works</dt>
            <dd>Pricing</dd>
            <dd>Membership</dd>
            <dd>FAQ</dd>
            <dd>Customer Support</dd>
          </dl>
        </div>
        <div class="span3 border-divider">
          <dl>
            <dt>About Us</dt>
            <dd>Founders</dd>
            <dt>Opportunities</dt>
            <dd>Jobs</dd>
            <dd>API / Developer support</dd>
          </dl>
        </div>
        <div class="span2">
          <dl>
            <dt><a href="//seocahill.com">Visit Our Blog</a></dt>
            <dd>
              <a href="//twitter.com/showkhill"><%= image_tag "twitter.png" %></a>
              <%= image_tag "facebook.png" %>
              <%= image_tag "rss.png" %>
              <%= image_tag "vimeo.png" %>
            </dd>
            <div class="row">
            </div>
          </dl>
        </div>
      </div>
      <div class="row footer-bottom">
        <div class="span12">
          Seo Cahill 2012
        </div>
      </div>
    </div>
  </footer>
</html>
